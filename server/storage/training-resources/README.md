Place real training resource files in this directory.

Expected filenames:
- `part66-guidebook.pdf`
- `avionics-fundamentals.pptx`
- `workshop-safety-checklist.pdf`

These are served by:
- `GET /api/training/resources/:resourceId/download`

Replace sample files with production files before go-live.
